"""
besoin de : fonction de hash (MD5, Whirlpool, SHA-256

fonction éponge

"""

def spongefunction(chain, length):
    blocs = p(flux)
    for bloc in blocs:
        state = hash(state >> bloc)

    for n in :
        sponge = concatenation(sponge, state)
        state = hash(state)

    return sponge

"""
for SHA3-256: r=1088
c=512
outputlength (bits) = 256
security level (bits) =128
mbits =01
d =0x06
"""

def hash_function():
    return

def hash_message(file_adress, hash_out_adress):
    return

def verify_hash(file_adress, hash_adress):

    return boolean